export const SETTINGS = {
    TILE_SIZE: 192,
    DEFAULT_ZOOM: 0.5,
    // 이동 속도는 StatManager의 'movement' 스탯으로부터 파생됩니다.
    // 자동 플레이 여부 (플레이어를 AI가 조종하도록 할지 여부)
    autoPlay: false,
    // ... 나중에 더 많은 설정 추가
};
